CONTENTS

nGram - This is a base class which holds information usefull for an 		arbitrary ngram.

DocReader - This is used as a helper class to read the input file.

LangGen - This is the main class, which both processess the text, creates 		    the n gram model, and generates text.




All necessary programs are already compiled, however if it is necessary to re-compile any class, open a terminal and run the command "javac className.java" 



@ Chris Betsill v01
